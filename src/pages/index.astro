---
import Layout from '../layouts/Layout.astro';
import barCenteredAiImage from '../assets/bar-centered-ai.png';
import chefImage from '../assets/chefs-2.png';
import bar1Image from '../assets/bar1.jpg';
import bar2Image from '../assets/bar2.jpg';
import restaurantImage from '../assets/restaurant.jpeg';
import barImageAI from '../assets/barAI.png';
import chefsImage from '../assets/chefs.png';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const slides = [
  { src: barCenteredAiImage.src, alt: "Sushi BBY bar" },
  { src: barImageAI.src, alt: "Sushi BBY bar 2" }
];

const galleryShots = [
  { src: bar1Image.src,       alt: "Interior — bar seating" },
  { src: bar2Image.src,       alt: "Evening service"        },
  { src: restaurantImage.src, alt: "Restaurant atmosphere"  },
  { src: chefsImage.src,      alt: "Chef at the counter"    },
];

const offerings = [
  {
    title: "Omakase",
    description: "A 13 course experience. No menu. Trust the chef.",
    image: barCenteredAiImage.src,
    imageAlt: "Omakase experience at Sushi BBY",
    href: "/reservations",
  },
  {
    title: "Tuna Carvings",
    description: "Whole bluefin broken down tableside — raw, ceremonial, and unforgettable.",
    image: bar1Image.src,
    imageAlt: "Tableside tuna carving",
    href: "/reservations",
  },
  {
    title: "Private Events",
    description: "The full counter, just yours. Intimate dining for groups up to twelve.",
    image: bar2Image.src,
    imageAlt: "Private dining at Sushi BBY",
    href: "/reservations",
  },
];
---

<Layout description="Omakase, tuna carvings, and private events at Sushi BBY.">
  <!-- Hero -->
  <section class="hero">
    {slides.map((slide, i) => (
      <div class={`hero-slide${i === 0 ? ' active' : ''}`}>
        <img src={slide.src} alt={slide.alt} />
      </div>
    ))}

    <div class="hero-overlay" aria-hidden="true"></div>

    <div class="hero-content">
      <h1>A Chef-Driven<br />Experience</h1>
      <p class="tagline">
        Omakase (お任せ) — "I'll leave it up to you." A multi-course progression
        built entirely around the day's catch. No menu. Trust the chef.
      </p>
      <div class="hero-actions">
        <div class="btn">
          <a href={base + '/reservations'} class="btn-primary">Reservations</a>
          <span></span>
        </div>
      </div>
    </div>

    <div class="hero-controls">
      <button class="hero-pause" aria-label="Pause slideshow" id="hero-pause">
        <svg class="icon-pause" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
          <rect x="3" y="2" width="3.5" height="12" rx="1"/>
          <rect x="9.5" y="2" width="3.5" height="12" rx="1"/>
        </svg>
        <svg class="icon-play" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
          <polygon points="3,1 13,8 3,15"/>
        </svg>
      </button>
      <div class="hero-dots" role="tablist" aria-label="Slideshow navigation">
        {slides.map((_, i) => (
          <button
            class={`hero-dot${i === 0 ? ' active' : ''}`}
            role="tab"
            aria-selected={i === 0 ? 'true' : 'false'}
            aria-label={`Slide ${i + 1}`}
            data-slide={String(i)}
          ></button>
        ))}
      </div>
    </div>
  </section>

  <!-- Chef Section -->
  <section class="home-chef">
    <div class="home-chef-media">
      <img src={chefImage.src} alt="Executive Chef" />
    </div>
    <div class="home-chef-text">
      <p class="home-chef-label">Executive Chef &amp; Owner</p>
      <h2>The Chefs</h2>
      <p>Trained in New Jersey. Refined in New York. Rooted here.</p>
      <div class="btn">
        <a href={base + '/chef'} class="btn-primary">Full Story</a>
        <span></span>
      </div>
    </div>
  </section>

  <!-- Signature Offerings -->
  <section class="home-offerings">
    <div class="home-offerings-inner">
      <div class="home-offerings-grid">
        {offerings.map((item) => (
          <a href={base + item.href} class="offering-card">
            <div class="offering-card-media">
              <img src={item.image} alt={item.imageAlt} />
            </div>
            <div class="offering-card-overlay" aria-hidden="true"></div>
            <div class="offering-card-body">
              <h3>{item.title}</h3>
              <p>{item.description}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Reserve CTA Band -->
  <section style="background: var(--color-bg); border-top: 1px solid rgba(240, 235, 226, 0.1); padding: 60px var(--pad-x); text-align: center;">
    <p style="font-family: 'Urbanist', sans-serif; font-size: 1.4rem; font-weight: 600; color: #fff;">
      Seatings are limited. Reserve yours.
    </p>
    <div class="btn" style="margin: 1.75rem auto 0;">
      <a href={base + '/reservations'} class="btn-primary">Reserve</a>
      <span></span>
    </div>
  </section>

  <!-- Gallery Teaser -->
  <section class="home-gallery-teaser">
    <div class="gallery-teaser-row">
      {galleryShots.map((shot) => (
        <div class="gallery-teaser-item">
          <img src={shot.src} alt={shot.alt} />
        </div>
      ))}
    </div>
    <div class="gallery-teaser-cta">
      <div class="btn">
        <a href={base + '/gallery'} class="btn-primary">View Gallery</a>
        <span></span>
      </div>
    </div>
  </section>
</Layout>

<script>
  const slides = Array.from(document.querySelectorAll<HTMLElement>('.hero-slide'));
  const dots = Array.from(document.querySelectorAll<HTMLButtonElement>('.hero-dot'));
  const pauseBtn = document.getElementById('hero-pause') as HTMLButtonElement | null;

  let current = 0;
  let paused = false;

  function goTo(index: number) {
    slides[current].classList.remove('active');
    dots[current]?.classList.remove('active');
    dots[current]?.setAttribute('aria-selected', 'false');
    current = ((index % slides.length) + slides.length) % slides.length;
    slides[current].classList.add('active');
    dots[current]?.classList.add('active');
    dots[current]?.setAttribute('aria-selected', 'true');
  }

  if (slides.length > 1) {
    setInterval(() => { if (!paused) goTo(current + 1); }, 6000);
  }

  dots.forEach((dot, i) => {
    dot.addEventListener('click', () => goTo(i));
  });

  pauseBtn?.addEventListener('click', () => {
    paused = !paused;
    pauseBtn.classList.toggle('is-paused', paused);
    pauseBtn.setAttribute('aria-label', paused ? 'Play slideshow' : 'Pause slideshow');
  });
</script>

// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://papagreeen.github.io',
    base: '/sushi-bby', // replace with your exact repo name

});
